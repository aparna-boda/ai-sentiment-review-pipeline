# ai-sentiment-review-pipeline
A production-style data pipeline that scores customer review sentiment using DistilBERT, loads results to Supabase PostgreSQL, and surfaces trends on a Streamlit dashboard.
